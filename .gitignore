*.img
*.img.*
*.tar.gz
*.pkg.tar.*
.env
/build/
/out/
secrets.env
*.secret

# Downloaded CI disk-image artifacts, worked on locally. Never committed.
.ci-artifact/
*.img
*.img.zst

# Generated from secrets.env. Never committed.
nix/secrets.nix
nix/out/
result
nix/out/
nix/bootc-bin/

# Upstream ALARM rootfs tarball, fetched by `make archlinuxarm-base`.
# ~800MB — never commit, CI re-downloads it fresh.
archlinuxarm-docker/*.tar.gz

# Local working clone of AlmaLinux/bootc-images-rpi, not part of this repo.
bootc-images-rpi/
archlinux-rpi/
sdcard-image/
